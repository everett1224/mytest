//C and C++ different 
//
//the .c file can't compile
//
//
#include <stdio.h>
int main()
{
	if(int i = 1)//in C error can't declare here ;C++ is good
		printf("dddd");

	for(int a = 1; a<10;);//in C can't pass compile can't declare here unless C99
	return 0;
}
